{
  "name": "card-battle-arena",
  "version": "1.0.0",
  "description": "Prototipo académico Card Battle Arena — temática Fullmetal Alchemist (fan-made, no comercial).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch src/data/db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "0.17.4",
    "vite": "^5.4.10"
  }
}